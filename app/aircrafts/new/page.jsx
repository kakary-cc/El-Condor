import React from "react";
import AircraftForm from "../../(components)/AircraftForm";

const Aircraft = () => {
    return (
        <div>
            <AircraftForm />
        </div>
    );
};

export const metadata = {
    title: "Condor - New Aircraft",
    description: "FBO Management System",
};

export default Aircraft;
